<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.2/font/bootstrap-icons.css">
   
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="./css/styles.css" rel="stylesheet"> 
    <script src="/js/main.js"></script>
    <title><%= title%></title>
   
</head>
<body>

    <div class="grid grid-cols-6">
        <div class="col-start-2 p-6">
            <a class='text-red-700 rounded-full border-2 p-2 bg-green-700 text-white' href="/dashboard">Dashboard</a>
       
        </div>
        <div class="col-start-5 p-6">
            <a class='text-red-700 rounded-full border-2 p-2 bg-blue-700 text-white' href="/signout">signout</a>

        </div>
    </div>
        <div class="grid grid-cols-6">
            <div class="col-start-2 col-span-4">
                <h1 class="text-center">Welcome the Election Ballot(Question/Voters)</h1>
                <h2 class="text-center">Election Name: <%=election.name %></h2>
                <div class="grid grid-cols-3 m-1">
                    <div class="col-start-2">
                    <div class="text-center">
                        <% if(messages && messages.error && messages.error.length > 0)  { %>
                            <% for(var i=0; i<messages.error.length; i++) { %>
                                
                                <p class="rounded-full bg-red-900 border-2 p-2 text-red"><%= messages.error.length%> <%= messages.error[i] %></p>
                            <% } %>
                        <% } %>
                    </div>
                    </div>
                </div> 
            </div>
           
            <div class="col-start-2 col-span-2 border-gray-700">
                
                
                   
                    <div class="border">
                    <h1 class="text-red-900 text-center">Question</h1>
                    <form action="/question/<%= election.id %>" method="post" class="border-2">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                        <div class="py-3">
                            <label for="title">Question Title:</label>
                            <input type="text" name="title" 
                            class="border border-gray-700 text-gray-900 rounded p-2 w-full">
                        </div>
                        <div class="py-3">
                            <label for="description">Description:</label>
                            <input type="text" name="description" 
                            class="border border-gray-700 text-gray-900 rounded p-2 w-full">
                        </div>
                        <div class="py-3">
                            <button type="submit" class="text-white bg-red-700 hover:bg-red-900 font-medium rounded px-5 py-2">Add New Election</button>
                        </div>
                    </form>
                    <%
                    if(election.Questions.length==0)
                    {
                    %>
                    <p>You have not created any Question yet in this election.</p>
                   
                    
                    <%
                    }
                    else{ 
                       %>
                      
                   
                       <%
                        for(let i = 0; i < election.Questions.length; i++) {
                        %>
                        <div class="p-3 border">
                            <h1 class="text-gray-900 text-center text-2xl">Question <%=i+1%></h1>
                            <div class="grid grid-cols-3">
                                <div class="bg-gray-300 p-2 m-1">
                                    <input type="text" value="<%= election.Questions[i].title %>" id="questionTitle-<%= election.Questions[i].id %>"/>
                                </div>
                                <div class="bg-gray-400 p-2 m-1">
                                    <input type="text" value="<%= election.Questions[i].description %>" id="questionDescription-<%= election.Questions[i].id %>"/>
                                   
                                </div>
                                <div class="bg-gray-100 m-1">
                                    <a href="#" onClick="QuestionUpdater(<%= election.id %>,<%= election.Questions[i].id %>)">
                                        <i class="bi bi-pencil-square"></i>
                                    </a>
                                    <%
                                    if(election.Questions.length>1) {
                                    %>
                                    <a href="#" onClick="QuestionDeleter(<%= election.id %>,<%= election.Questions[i].id %>)">
                                    <i class="bi bi-x-octagon-fill"></i>
                                    </a>
                                    <% }%>
                                </div>
                                
                            </div>
                            <form action="/option/<%= election.id %>/<%= election.Questions[i].id %>" method="post" class="border-2">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                                <div class="grid grid-cols-3">
                                            <div class="col-span-2">
                                                <input class="border border-gray-700 text-gray-900 rounded p-2 w-full" type="text" name="title" placeholder="Option">
                                            </div>
                                            <div>
                                                <button class="px-2 py-2 m-1 font-semibold text-sm bg-green-500 text-white rounded-none shadow-sm float-right"><span><i class="bi bi-plus-circle-fill"></i></span>&nbsp;Add</button>
                                            </div>
                                    
                                </div>
                            </form>
                            <ul>
                                <% for(let j = 0; j < election.Questions[i].Options.length; j++)
                                { %>
                                <li class="bg-grey-700 p-2 border border-white">
                                    <div class="grid grid-cols-4 border-2 p-3">
                                        
                                        <div class="">
                                            Option <%= j + 1 %>
                                        </div>
                                        <div class="text-blue-900 float-right">
                                            <input type="text" value="<%= election.Questions[i].Options[j].title %>"
                                id="optionTitle-<%= election.Questions[i].Options[j].id %>"/>
                                        </div>

                                        <a href="#" onClick="optionUpdater(<%= election.id %>,<%= election.Questions[i].id %>, <%= election.Questions[i].Options[j].id %>)">
                                            <i class="bi bi-pencil-square"></i>
                                        </a>
                                        <%
                                        if(election.Questions[i].Options.length>1) {
                                        %>
                                        <a href="#" onClick="optionDeleter(<%= election.id %>,<%= election.Questions[i].id %>, <%= election.Questions[i].Options[j].id %>)">
                                        <i class="bi bi-x-octagon-fill"></i>
                                        </a>
                                        <% }%>
                                        
                                        
                                        
                                       
                                        </a>
                                    </div>
                                </li>
                                <% } %>
                          </ul>
                            
                           

                        </div>
                    <%
                        }
                    %>
                       
                        <%
                    }
                    %>
                </div>
            </div>
            <div class="col-start-4 col-span-2 border-gray-700">
                
              
                    
                    <div class="border">
                    <h1 class="text-purple-900 text-center">Voters</h1>
                    <form action="/voters/<%= election.id %>" method="post" class="border-2">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                        <div class="py-3">
                            <label for="voter_id">Voter ID:</label>
                            <input type="text" name="voter_id" 
                            class="border border-gray-700 text-gray-900 rounded p-2 w-full">
                        </div>
                        <div class="py-3">
                            <label for="password">Password:</label>
                            <input type="text" name="password" 
                            class="border border-gray-700 text-gray-900 rounded p-2 w-full">
                        </div>
                        <div class="py-3">
                            <button type="submit" class="text-white bg-purple-700 hover:bg-purple-900 font-medium rounded px-5 py-2">Add New Voter</button>
                        </div>
                    </form>
                    <%
                    if(election.Voters.length==0)
                    {
                    %>
                    <p>You have not added any Voters yet in this election.</p>
                   
                    
                    <%
                    }
                    else{ 
                       %>
                       <table class="table-auto border border-collapse border-slate-400 max-w-prose">
                        <tr>
                            <th class="border border-slate-300">S.No.</th>
                            <th class="border border-slate-300">Voter ID</th> 
                            

                        </tr>
                   
                       <%
                        for(let i = 0; i < election.Voters.length; i++) {
                        %>
                        <tr>
                            <td class="border border-slate-500"><%=i+1%></td>
                            <td class="border border-slate-500"><%=election.Voters[i].voterId%></td>
                        </tr>
                    <%
                        }
                    %>
                       </table>
                        <%
                    }
                    %>
                </div>
            </div>
       
        </div>
       
    </body>
</html>